# 项目协作规则

本文件适用于本项目根目录及其所有子目录。除非用户另有明确指令，必须遵守以下规则。

## “执行最新需求”工作流

当用户发送“执行最新需求”时，必须严格依次执行以下四个阶段。不得因为需求看似简单而跳过阶段、批准节点或报告要求。用户对某一阶段的批准不代表对后续阶段的批准。

### 第一阶段：检查和计划

仅执行只读检查，不得修改业务代码、测试、配置、依赖、数据库或其他项目文件。

必须读取并检查：

- `docs/CURRENT_REQUIREMENT.md`
- 本 `AGENTS.md` 以及适用于目标文件的其他项目规则
- 与需求相关的源代码
- 与需求相关的测试
- 与需求相关的配置和文档
- 当前 Git 状态，包括已有的未提交修改

如果必读文件不存在、内容不完整或验收标准不明确，必须如实说明，不得臆造其内容；在不越权的前提下完成其余只读调查，并将缺失信息列为阻塞项、风险或假设。

在理解需求和验收标准、调查现有实现后，输出一份简洁且可执行的修改计划。计划必须明确说明：

- 准备修改哪些文件
- 每个文件准备修改什么
- 涉及哪些类、函数、接口或配置
- 为什么需要修改
- 需要增加或修改哪些测试
- 准备运行哪些测试和检查
- 是否影响接口、数据库、配置、依赖或兼容性
- 存在哪些风险和假设
- 明确不会修改哪些内容

输出计划后立即停止，不得开始实施，并在回复末尾单独显示：

`等待批准修改`

只有收到用户明确指令“批准修改”后，才能进入第二阶段。其他表达不得视为批准。

### 第二阶段：代码实施

收到“批准修改”后，按照已经展示并获批的计划实施修改。只允许修改计划中明确列出的文件和内容。

实施时必须遵守：

- 采用范围尽量小、容易维护的修改
- 遵循项目现有架构和代码风格
- 避免与当前需求无关的重构
- 新增功能必须增加测试
- 缺陷修复必须增加回归测试
- 不得覆盖、还原、删除或混入用户已有的未提交修改
- 不得执行破坏性的 Git 命令
- 不得写入密钥、密码或真实账号
- 不得自动提交、推送、创建 Pull Request、合并或部署

如果实施过程中发现必须修改计划之外的文件、接口、数据库、依赖、配置或业务行为，必须立即停止，不得先行修改，并向用户说明：

- 需要增加修改的文件
- 准备修改的内容
- 修改原因
- 不修改造成的影响
- 可能产生的风险

然后在回复末尾单独显示：

`等待批准范围变更`

只有收到用户明确指令“批准范围变更”后，才能按说明扩展范围并继续实施。若新增范围再次变化，必须重新请求批准。

### 第三阶段：测试和代码审查

完成修改后，必须运行项目现有且与需求相关的测试、构建、格式检查、静态检查和类型检查。报告中的结果必须来自真实执行；任何无法执行、失败或跳过的检查都必须说明具体原因，不得声称未实际运行的检查已经通过。

随后检查完整 Git Diff，确认本次修改没有覆盖用户已有改动，也没有包含计划外变更。向用户报告：

- 修改了哪些文件
- 每个文件具体修改了什么
- 每项修改对应哪个需求
- 实际运行的测试或检查命令及其结果
- 是否存在失败、无法执行或跳过的检查，以及原因
- 接口、数据库、配置和依赖是否发生变化
- 已知限制和风险
- 需要用户重点检查的代码位置
- Git Diff 统计

修改完成后保持代码处于未提交状态，方便用户在 Codex 中逐文件查看 Diff。不得自动提交。

完成报告后立即停止，并在回复末尾单独显示：

`等待批准提交`

### 第四阶段：提交

只有收到用户明确指令“批准提交”后，才能创建 Git 提交。其他表达不得视为批准。

提交前必须再次检查当前 Git 状态和完整 Diff，确认它们与第三阶段已经报告的内容一致。只提交本次需求相关文件，不得包含用户原有修改、计划外文件或其他任务的修改。

创建本地 Git 提交后，报告：

- 提交哈希
- 提交包含的文件

提交后不得自动推送、创建 Pull Request、合并或部署；这些操作分别需要用户另行明确授权。

## 当前请求边界

创建或更新本文件本身不等于触发“执行最新需求”。只有用户实际发送该触发指令时，才开始上述工作流。

## 项目目录与路径规则

- 使用 Conda 虚拟环境 `AFC` 运行、测试和验证本项目。
- 主程序源码放在 `src/project/`，不得把业务数据或运行输出放入源码包。
- `tests/` 和 `scripts/verification/` 是预留目录；当前按用户要求不包含测试或验证代码。未来只有在明确需求要求时才能重新添加。
- 静态样例数据放在 `data/sample/`，可变运行状态放在 `data/runtime/`。
- 本机应用配置放在 `config/`，正式案例配置放在 `profiles/`，派生 profile 缓存放在 `profiles/cache/`。
- 程序生成的表格、图片、日志和临时结果统一放在 `output/`，不得重新散落到项目根目录。
- 文档放在 `docs/`；不参与当前运行的历史源码仅放在 `archive/`。
- 新增路径必须优先使用 `pathlib`，并基于 `src/project/shared.py` 中的项目根目录常量定位，不得依赖当前工作目录。
- 外部实验数据目录通过用户配置指定，不得在源码中新增机器相关的绝对路径。
- `config/app_config.json`、`data/runtime/`、`profiles/cache/`、`output/` 和 Python/pytest 缓存属于本机可变内容，不纳入版本管理。
